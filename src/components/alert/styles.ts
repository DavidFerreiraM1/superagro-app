import Styled from 'styled-components/native';

export const AlertBackground = Styled.View`
  background-color: rgba(0,0,0,0.5);
  height: 100%;
  width: 100%;
  position: absolute;
  z-index: 200;
`;
